# Serenity Salon & Spa — Progress Tracker

> Updated at the end of each working step. Plan lives in `plan.md`.

## Status legend
- `[ ]` not started
- `[~]` in progress
- `[x]` done

## Steps

1. `[x]` **Plan approval & docs** — `plan.md` written, `progress.md` created. 2026-08-19
2. `[x]` **Migrations 009+ & types & seed** — banks, bundles, material_sales, payments, group_visits, feedback, uploads folder, role renames, daily queue reset. 2026-08-19
3. `[x]` **Payments overhaul** — `payments` API, checkout modal (cash/bank, split, cashback, bank dropdown, txn, receipt attach/compress, advanced collapsible, discount-at-checkout), `PrintableInvoice` updates. 2026-08-19
4. `[x]` **Queue ordering + 3-digit numbers + TV + reassign** — POS sort (completed-unpaid then newest), `Q-001` daily per branch, TV shows queue number, per-service reassign blocked on completed. 2026-08-19
5. `[x]` **Retail / material sales** — Retail tab, cart, separate tables, stock deduction at payment, locked selling price, no commission, shares payment modal. 2026-08-19
6. `[x]` **Roles rework** — `owner`/`manager`/`reception`, guards, seed accounts, Tenant Admin UI updates. 2026-08-19
7. `[x]` **Analytics filters** — ReportsDashboard category/staff/channel + date/branch filters; split-payment-aware KPIs via new read-only ledger listing (`GET /api/payments` company-scoped, filters) and `GET /api/reports/payments` channel aggregate. 2026-08-19
7b. `[x]` **Deployed-app bug fixes** — (1) Switch Employee failed with "authentication required" because `StaffPortalView` called raw `fetch` without the bearer token; now uses `apiFetch`. (2) Completed-unpaid visits were hidden from Reception POS by the today-only filter (and sessions finished via the staff flow never set `completed_at`); POS now always surfaces `completed && !isPaid` sessions and the status endpoints stamp `completed_at`. (3) Checkout modal folds discount + cashback into a collapsed "Advanced" section, drops the running-totals grid in favor of a single inline "Remaining to allocate" hint, replaces "Add split payment line" with a **Cash / Bank / Cash + Bank** method selector, removes the "Attach receipt" upload entirely, auto-fills amounts (manual amount inputs only appear in the Cash + Bank split), and shows only a single Total row (subtotal/tax/net-due removed). Rebuilt, redeployed (files placed in correct subdirs), verified live: `verify-pin` 200 with correct PIN; "Attach receipt" gone from all served bundles. 2026-08-19
7c. `[x]` **Reception POS simplified session flow** — Took the inline "Create New Session" builder off the main dashboard and turned it into a two-step modal. Top "New Client Session" button removed (header now only shows the Live badge + brand + logout); the tabs-row action is renamed **Add Customer**; the empty-state button is now **Add First Customer**. The modal opens with **Step 1 (New Customer Session)**: pick existing customer or register a new one (reuses client-picker dialog), choose services by category, staff auto-suggested per service with changeable dropdown, running cart with total. **Step 2 (Session Summary)** is an independent confirmation screen showing customer + loyalty points, per-service rows (staff, duration, price), and net total, with Back / **Confirm Queue** buttons. `createSession` resets the modal to step 1 on success. Lint, 35 tests, and build all green; deployed and verified in the served bundle (new labels present, "Hide Builder"/"Add Session" gone; pm2 `gech-salon` id=2, restart count 6). 2026-08-19
7d. `[x]` **Form simplifications + queue UX batch** — (1) "Register New Client" split from the client-picker dialog no longer asks for email or preferences/notes (Full Name + Phone only). (2) Every phone input across the app now defaults to the `+251 ` prefix (Reception POS, Retail tab, Staff portal walk-in, Appointment booking modal, Tenant Admin staff/branch, SaaS admin company) and guards strip/blank a bare `+251` so no empty number is persisted. (3) Reception POS: selected-services cart is now a high-visibility bordered/filled block with a bold "Selected Services (n)" header + running total; Step 2 Session Summary rebuilt with rounded cards, bold header rows and a large Confirm button. (4) Reception table filters are now bold pill buttons with tallies: **All / On Queue / In Progress / Unpaid / Done / Cancelled** (with search box above and Clear/Reset actions); the old "Unpaid Only" toggle is gone. (5) Queue board, TV, and staff portal all group sessions **by customer**: each row shows the queue number + customer name with their services stacked underneath; TV "Now Serving"/"Next Up" show queue number + customer; staff portal lists per-service Start/Complete actions under one customer card. (6) Removed the **Sign In** button from the public website (desktop header, mobile drawer, footer) and dropped the now-unused `onOpenLogin` prop. Lint, 35 tests, and build all green; deployed and verified in served bundles (pm2 restart count 7). 2026-08-19
7e. `[x]` **Queue-number timezone bug fix** — The daily-queue test began failing after ~21:00 local time ("expected 1 to be 2", both sessions getting Q-001). Root cause: `nextQueueNumber` computed `dayStart`/`dayEnd` as UTC ISO strings but the `TIMESTAMP` columns are stored in the DB session timezone (`Africa/Nairobi`, UTC+3), so sessions created after 21:00 local fell outside the "today" window and every new session repeated Q-001. Fixed by comparing against `CURRENT_DATE` / `CURRENT_DATE + INTERVAL '1 day'` inside SQL (consistent with `CURRENT_TIMESTAMP`-backed `created_at`). All 35 tests green again; rebuilt/redeployed (pm2 restart count 7). 2026-08-19
7f. `[x]` **Overview landing tab + KPI unification + admin bug fixes** — Added **Overview** as the default admin landing tab (`AdminTab` union + first `ADMIN_TABS` entry in `src/lib/adminNav.ts`; `AppLayout` sidebar default switched from `'branches'` to `'overview'`). Overview block in `TenantAdminView` shows: scope Select (All Branches + per-branch, defaulting to the sidebar's branch with sidebar-sync effect), 6 KPI cards (**Revenue Today, Completed Today, Active Queue, Net Today, Unpaid Sessions, Low Stock Items**), **Needs Payment** + **Low Stock Alerts** panels (jump to financials/inventory tabs), **Live Queue** preview (jump to Branches), and **Quick Actions** (Add Staff / Create Service / Record Expense / Add Stock / View Reports — wired to existing modal setters + `setAdminTab`). Created shared `src/lib/kpi.ts` (`COLORS`, `completedNetTotal`, `revenueOn`, `activeQueue`, `unpaidCompleted`, `lowStockItems`, `channelTotals`, `staffAggFromVisits`, `commissionSum`, `servicePopularity`, `categoryRevenue`) and made it the single calculation path for Overview + Reports + Staff Performance. ReportsDashboard: dropped dead `commissionLogs` prop, adopts shared helpers, `useServerSummary = !hasServiceFilter && !hasPaymentFilter` so filtered views share one local-computation path, CSV export now forwards `completed=true`/`serviceCategory`/`staffId` to the server endpoint (new optional `staffId`/`serviceCategory`/`completed` filters in `server-lib/reports.ts` with EXISTS subqueries on `visit_session_services` + `services`). Bug fixes: stray "Category" input removed from Add Staff modal, users-filter now a fixed companyUsers constant (dead branch dropdown), raw `alert()` → `showToast('success', ...)`, audit-table IP fallback `'—'`, unused lucide imports removed. Deleted orphaned `WeeklyScheduler.tsx` (zero references). Lint, 35 tests, and build all green; deployed + verified (HTTP 200, Overview/Quick Actions/Needs Payment/Low Stock Alerts/Live Queue labels in served bundle, `kpi-*.js` chunk present, WeeklyScheduler gone, pm2 `gech-salon` id=2 restart count 9). 2026-08-19
7g. `[x]` **Staff page redesign (renamed from "Staff Roster")** — Sidebar nav label `Staff Roster` → **Staff** in `src/lib/adminNav.ts`. Rebuilt the staff tab in `TenantAdminView`: new **scope Select** (All Branches + per-branch, defaulting to the sidebar branch with sidebar-sync `staffScopeBranchId` state, same pattern as the Overview scope); **4-card KPI strip** (Staff / On Shift / Busy Now with % occupancy / Commissions Accrued); **enhanced table** adds **Live** column ("Serving {customer} · queue number" from in-progress sessions via `scopeInProgressByStaff`, else "Available") plus per-staff **Sessions Done / Revenue / Commission Earned** figures sourced from the shared `staffAggFromVisits` helper (`src/lib/kpi.ts`); commission-logs section retitled **Commission Logs & Payout Schedule**, now scoped (total + table respect the selector, sourced from `scopeCommissions`/`scopeCommissionTotal`); removed then-unused `branchStaff`/`branchCommissions`/`totalCommissionsEtb` locals. No server changes. Lint, 35 tests, and build all green; deployed + verified (HTTP 200, "Commissions Accrued"/"Sessions Done"/"On Shift"/"Busy Now" in served bundle, "Staff Roster & Commission Rules" gone, "Staff" sidebar label confirmed in nav chunk, zero "Staff Roster" strings in served bundles, pm2 `gech-salon` id=2 restart count 10). 2026-08-19
7h. `[x]` **Staff page: payout actions + simpler layout** — Incorporated commission payouts into the Staff page and simplified it further. Staff tab now: header row merges the **Scope Select** + "Add Staff Member" button; **3-card KPI strip** (Team Size with on-shift/busy subcounts, Occupancy %, **Pending Payouts ETB** with unpaid-record count — new `scopePendingPayouts` computed from non-paid `scopeCommissions`); staff table slimmed from 10 → 7 columns (drops Live/Sessions Done/Revenue/Commission Earned; Branch column shown only in "All Branches" scope; "Serving {customer}" folded into the Status cell; new right-aligned **Pending Payout** per-staff via `payoutDueByStaffId`). The commission-logs block became **Commission Payouts**: per-row status advancement (Unpaid → Requested via amber clock, → Paid via green check, paid rows can reset to unpaid via refresh icon) calling `PATCH /api/commission-logs/payout` through new `handleUpdatePayoutStatus` handler (`payoutUpdatingId` busy-state, `showToast` on success/error, then `onRefresh?.()`); `onRefresh?: () => Promise<void>` prop added to `TenantAdminViewProps` and wired to `fetchDbState` in `App.tsx` (line ~342). Removed now-unused `scopeStaffAgg` + `staffAggFromVisits` import. This closes the gap where the payout-status endpoint was never called from any UI (previously only display-only "Request Payout" local state in `StaffPortalView`). Lint, 35 tests, and build all green; deployed + verified (HTTP 200, "Commission Payouts"/"Pending Payouts"/"Team Size"/"Occupancy"/"Mark as Paid"/"Mark as Requested"/"Reset to Unpaid" in served bundle, old "Commissions Accrued"/"Sessions Done" gone, pm2 `gech-salon` id=2 restart count 11). 2026-08-19
7i. `[x]` **Payout = accepted amount form, not per-service table** � Removed the per-record "Commission Payouts" table and `handleUpdatePayoutStatus`; staff-table rows gained an emerald **Pay** button (label + icon, sits in the Pending Payout cell rather than the Actions column) that opens a fixed-overlay modal showing the staff's **total unpaid commission** plus a form for the **"Amount Accepted (ETB)"** at that moment (with optional notes) and a Confirm button. Server `PATCH /api/commission-logs/payout/batch` now takes `{staffId, companyId, amountAcceptedEtb, notes?}`: it pays the **oldest unpaid commission logs** in created_at order up to the accepted amount (a log is indivisible � if the amount can't cover it fully it stays unpaid for a later payout), writes one row to the new **`commission_payouts`** table (migration 008) recording `amount_accepted_etb`, `logs_paid`, notes, and inserts an audit log; returns `{payoutId, amountPaidEtb, logsPaid}`. Anything not covered stays unpaid � partial settlements are fully supported. Older Daily/Weekly/All window logic (`payoutPeriod`, `payoutCutoffFor`, `staffPayoutFor`) removed entirely. Test rewritten: seeds two logs for `stf_meron_02`, verifies paying 1120 marks only the oldest paid + payout row recorded (amount/logs/notes), paying the remaining 200 completes it, then 400 on nothing-left/negative/missing-amount. Lint, **36 tests**, build all green; deployed + verified (md5s match, migration applied + `commission_payouts` exists, "Amount Accepted (ETB)" in served TenantAdminView chunk, endpoint returns 400 on bad/negative amount + 401 unauthenticated live, pm2 `gech-salon` id=2 restart count 14). 2026-08-20
8. `[x]` **Walk-in tablet + feedback** — New public **`/tablet`** route (`WalkInTabletView`, touch-first kiosk) + public backend endpoints in `server-lib/routes/pos.ts` (registered before the `posOnly` gate): `POST /api/public/tablet/walkins` (phone required, name optional, optional service+staff; find/create-by-phone; `nextQueueNumber` Q-001 ticket; if service+staff picked and that staff is free the line+session start `in_progress`), `GET /api/public/tablet/visits?companyId&branchId&phone` (recent visits with service names for pick-any-visit feedback), `POST /api/public/tablet/feedback` (per-session rating 1–5 + complaint + anonymous). Login screen's "Register as a Customer (no login required)" now links to `/tablet` (was the interim `/?register=1` booking-modal open, which was removed). New owner/manager **Reviews & Complaints** admin tab (`/api/feedback`, `mgmtOnly` in finance.ts — reception gets 403) lists stars, complaint text, customer (hidden when anonymous), ticket, branch, date. New dedicated describe block — walk-in without service issues a big Q ticket, phone-not-supplied → 400, customer-by-phone reuse, service+staff line attached, visit lookup + missing-param 400, per-visit feedback accepted + visible via `/api/feedback`, rating out-of-range/missing → 400, anonymous hides identity, reception 403. **46 tests** green (was 36), lint + build green; deployed (sources md5-verified, "Get My Ticket"/"Reviews & Complaints"/"Rate This Visit" in served bundles, `/tablet` → 200, live smoke: walkins 400 guard / feedback public 400 / feedback list 401 unauthenticated; pm2 `gech-salon` id=2 restart count 16). 2026-08-20
8b. `[x]` **Mobile responsiveness pass + repo push** — Surveyed every view for mobile issues (grid prefixes, table wrappers, tab bars, toolbars, dialogs). Findings: shadcn `Table` already wraps in `overflow-x-auto`, all `DialogContent`s are `max-w-[calc(100%-2rem)]`, most grids already collapse (`grid-cols-1 md:*`), ArchitectBlueprintView raw tables already wrapped, Reports filter bar already wraps. Fixes applied: (1) `ui/tabs.tsx` TabsList now `max-w-full overflow-x-auto` with hidden scrollbar so every tab bar (Reception POS 6 tabs, Staff portal, Login) scrolls instead of overflowing on phones; (2) ReceptionistPos header controls wrap and Sign Out hides the staff name below `md`; title row wraps; (3) StaffPortalView header wraps (`flex-wrap` + gap-y); (4) LoginScreen demo-credential grid stacks on phones (`grid-cols-1 sm:grid-cols-2`); (5) `index.css` forces 16px form-control font-size ≤640px to stop iOS Safari focus-zoom. tsc, build, unit tests green (integration suite skips locally — no Postgres/Docker on the dev machine; server code untouched). Committed `fdaf601` (walk-in tablet/feedback/payments/retail/catalog batch) + this pass on `master`; remote push done manually by user (no stored GitHub credentials / no gh CLI on this machine). 2026-08-21
9. `[ ]` **Bundle services** — manager/owner CRUD, POS expansion, proration on partial use.
10. `[ ]` **Groups (special service)** — group creation, members as queue tickets, group checkout w/ flexible total + discount, one payment.
11. `[ ]` **SMS wiring + commission precedence + final QA** — events to sms service (`log`), service-rule-wins + gross base, `npm run lint` + tests.

## Notes / decisions log
- Queue reset scope: **per branch per calendar day**.
- Receipt attachment: local server, compressed.
- Payments split: per-line amounts (cash/bank each with own txn ref).
- Commission: service rule wins; gross price base.
- Analytics channel KPIs are ledger-driven (`payments` rows), so mixed cash/bank invoices and retail/group payables are counted correctly.
- Weekly-report "ear" image feature dropped — referenced assets (`ПриSunsPkix_perf.png`, `report-ear-experiment.png`) no longer exist in the repo and the overview/report work supersedes it. 2026-08-19

## Test coverage (2026-08-19)
- 47 tests green across 4 files; coverage now includes: banks CRUD + role gate (owner ok / reception 403), company-scoped + filtered payment ledger listing, channel breakdown endpoint, daily sequential queue numbers, reassign guard (open ok / completed 409), owner-role mgmt access, commission batch-payout (flips unpaid→paid, idempotent with `from` window, 400 validation), walk-in tablet registration (Q-XXX ticket, phone-required 400, find/create-by-phone, service+staff line), public visit lookup + 400 guard, per-visit feedback (rating bounds validation, anonymous identity hiding, owner-view, reception 403), public tablet catalog (services + staff per branch, reception/manager excluded, 400 guard).

## Known environment facts (for later sessions)
- Node at `C:\Program Files\nodejs` (winget). Win PowerShell must call `node_modules` scripts with `node` on PATH; execution policy blocks `.ps1` npm shims, use `npm.cmd`.
- PostgreSQL: portable binaries in `C:\Users\gemg2\AppData\Local\Programs\postgres\17`, data dir `...\17\data`, running on `127.0.0.1:5432`, trust auth, DB `gech_salon_db`.
- Start postgres: `pg_ctl -D "C:\Users\gemg2\AppData\Local\Programs\postgres\17\data" -l <logfile> start`.
- Dev server start: `cmd /c "set PATH=C:\Program Files\nodejs;%PATH% && npm run dev"` in repo root; logs at `dev.log`.
- Repo root: `C:\Users\gemg2\OneDrive\Documents\New folder\salon`.